{
  "name": "spatch",
  "version": "2026.03.106",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite build && vite preview",
    "clean": "rm -rf dist",
    "test": "bun test && bun run test:e2e",
    "test:unit": "bun test",
    "test:e2e": "TEST_PORT=$(get-port) bunx playwright test",
    "test:e2e:docker": "docker run --rm -v \"$PWD\":/work -w /work tiene/frontend-ci-image bash -c 'mise install && bun install --frozen-lockfile && bun run test:e2e'",
    "check": "tsc --noEmit",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "dependencies": {
    "@preact/signals-core": "^1.13.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@tabler/icons": "3.38.0",
    "get-port-cli": "^3.0.0",
    "happy-dom": "^20.8.3",
    "lefthook": "^2.1.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  },
  "trustedDependencies": [
    "lefthook"
  ]
}
