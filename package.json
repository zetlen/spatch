{
  "name": "sigil-synth",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --dev --watch --serve",
    "clean": "rm -rf dist",
    "serve": "cd dist && bunx serve"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
